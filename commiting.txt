feat: add LiveKit call transcription and refactor app structure

Extend the simple LiveKit video room prototype with backend-driven live
transcription, local transcript persistence, and a cleaner modular backend
layout that is easier to maintain as the project grows.

Video room and call experience:
- keep the existing shareable room-link flow for multi-user video calls
- preserve the plain HTML, CSS, and vanilla JS frontend
- continue publishing local camera and microphone tracks through LiveKit
- continue rendering remote participant media in a responsive room view

Live transcription:
- add a transcription pipeline that starts per published local audio track
- add `POST /api/transcription/start-track` to create a transcription job
- add `POST /api/transcription/stop-track` to stop a running transcription job
- add `GET /api/transcripts/{roomId}` to return saved transcript backlog
- add `/ws/transcription-ingest` for LiveKit track-egress audio ingestion
- add `/ws/transcripts/{roomId}` for live caption delivery to room clients
- bridge LiveKit track egress audio to OpenAI realtime transcription on the server
- persist finalized speaker-tagged transcript entries to
  `transcripts/{roomId}/events.jsonl`
- write a readable `transcript.txt` export alongside the JSONL event log
- show live captions and transcript backlog directly in the room UI
- allow transcription failures to degrade captions only while keeping the call usable

Backend refactor:
- split the oversized `main.py` into focused modules under `app/`
- move environment and path handling into `app/config.py`
- move request and response models into `app/schemas.py`
- move room creation and token helpers into `app/livekit_service.py`
- move transcription job lifecycle, persistence, and websocket handling into
  `app/transcription.py`
- move shared helpers into `app/utils.py`
- move FastAPI app creation and route definitions into `app/server.py`
- reduce `main.py` to a thin entrypoint and compatibility export surface

Frontend updates:
- add a transcript side panel to the room page
- fetch transcript history after join so late participants see prior captions
- open a live caption websocket while the room is active
- start transcription after the local audio track is published
- stop transcription when the participant leaves the room

Configuration and documentation:
- add `.env.example` with all required and optional runtime variables
- document the full project structure in `README.md`
- expand setup docs to cover transcription-related environment variables
- document the requirement that the app must be reachable by LiveKit egress
  when using remote LiveKit deployments

Testing and dependency updates:
- keep the FastAPI test suite green after the refactor
- extend tests for transcription start, transcript retrieval, and missing
  OpenAI configuration handling
- add `aiohttp` as a runtime dependency for the realtime transcription bridge
- refresh `uv.lock` to match the current dependency set
