services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: aidebate-prototype:latest
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file:
      - .env
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
